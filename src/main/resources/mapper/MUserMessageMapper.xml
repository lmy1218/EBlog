<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lmy.eblog.mapper.MUserMessageMapper">

    <select id="selectMessgeVo" resultType="com.lmy.eblog.vo.UserMessageVo">
        select m.*, (select username from m_user where id = m.from_user_id) as fromUserName,
                    (select title from m_post where id = m.post_id) as postTitle
            from m_user_message m
            ${ew.customSqlSegment}
    </select>
</mapper>
